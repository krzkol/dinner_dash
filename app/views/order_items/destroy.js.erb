$('.delete-order-item').bind('ajax:success', function() {
  $(this).closest('.row').fadeOut('slow', function() {
    $(this).closest('.row').remove();
  });
  if(!<%= cart_has_items? %>){
    $('#cart-section').fadeOut('slow');
  }
});
