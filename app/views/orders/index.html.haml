%h1 Your orders
- if @orders.any?
  %table.table.table-striped
    %tr
      %th ID
      %th Ordered
      %th Total
      %th Status
      %th
    - @orders.each do |order|
      %tr
        %td= order.id
        %td= order.created_at
        %td= order.total
        %td= order.status
        %td= link_to 'Show', order
- else
  %h3 Currently not have any orders
